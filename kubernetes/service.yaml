# kubernetes/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: starbucks-svc
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: starbucks
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000

